const config = {
  environment: '${environment}',
  graphqlApiURL: '${hasura_url}',
  authFnURL: '${functions_auth_url}',
  certificateFnURL: '${functions_certificate_generator_url}',
  auth: 'oauth',
  oauth: {
    clientId: '${oauth_client_id}',
    hostedDomain: '${gsuite_domain}'
  }
}

export default config
